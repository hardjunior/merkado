<?php
if (basename($_SERVER["PHP_SELF"]) == "senha.php") {
	die("<center ><h1>Este arquivo não pode ser acessado diretamente.");
}
   $usuario   = 'automatico@merkadorestaurante.pt';    // SMTP username
   $Password   = '2020merkado';                        // SMTP username